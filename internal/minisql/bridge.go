package minisql

// BridgeSQLFromIframe is reserved for a future host-mediated SQL path for iframe-only apps
// that cannot run a package binary. Prefer TALOS_SQL_DSN on the manifest binary process first.
